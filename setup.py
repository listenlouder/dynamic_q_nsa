from setuptools import setup

setup(name='dynamic_q_nsa',
      version='0.1.0',
      description='Gathers matchmaking info for high elo games',
      install_requires=[
          'riotwatcher',
      ])